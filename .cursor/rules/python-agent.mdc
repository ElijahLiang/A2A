---
description: FastAPI Agent Runtime Python 编码规范
globs: apps/agent/**/*.py
alwaysApply: true
---

# Python Agent 规范 (FastAPI)

## 项目约定

- Python 3.10+（推荐 3.12），使用 type hints
- 依赖：`apps/agent/requirements.txt` + venv（非强制 uv）
- 入口：`apps/agent/main.py`（`uvicorn main:app`）
- 异步优先：DB / Redis / LLM I/O 使用 async/await

## 目录（当前）

```
apps/agent/
  main.py          # FastAPI 应用
  config.py        # 环境变量（加载根 .env）
  db/              # pg_client、redis_client
  llm/gateway.py   # 按 agent 路由 Key
  agents/          # scheduler、memory、persona…
  social/          # letters、meetup
  security/vault.py
```

## LLM 调用

- 通过 `llm/gateway.py` 获取 client（系统 Key / 用户 Key）
- 勿在业务模块散落硬编码 API Key
- 无 Postgres 时允许内存降级（见 `db/pg_client.py`）

## 配置

- `DEEPSEEK_API_KEY` / `DATABASE_URL` / `REDIS_URL` 从环境或根 `.env` 读取
- 敏感信息禁止提交进仓库
