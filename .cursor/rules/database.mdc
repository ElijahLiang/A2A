---
description: 数据库操作与 Migration 规范
globs: apps/api/src/repositories/**/*.ts,apps/api/migrations/**/*.sql,apps/agent/db/**/*.py
alwaysApply: false
---

# 数据库规范

> 当前 V2：Schema 由 [`apps/agent/db/pg_client.py`](apps/agent/db/pg_client.py) 在连接时初始化。下方 migration 约定面向未落地的 `apps/api`。

## Migration

- 使用 dbmate 或 node-pg-migrate 管理迁移
- 文件命名: `YYYYMMDDHHMMSS_description.sql`
- 每个 migration 必须有 UP 和 DOWN
- 禁止修改已提交的 migration 文件

## 查询规范

```typescript
// ✅ 参数化查询
const result = await db.query(
  'SELECT * FROM users WHERE id = $1 AND status = $2',
  [userId, 'active']
);

// ❌ 字符串拼接
const result = await db.query(`SELECT * FROM users WHERE id = '${userId}'`);
```

## JSONB 字段

- JSONB 字段在代码层定义 TypeScript 类型
- 使用 JSONB 操作符查询，不要全量取出后在代码中过滤

## 索引

- 所有外键列必须有索引
- 高频查询字段创建复合索引
- 向量索引使用 ivfflat，lists 参数 = sqrt(行数)
