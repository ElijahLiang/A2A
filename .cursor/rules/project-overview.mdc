---
description: A2A 项目总体规范与上下文，每次对话都必须了解
alwaysApply: true
---

# A2A 项目概览

A2A (Avatar To Avatar) 是一款 AI Agent 驱动的线下社交撮合平台。

## 技术栈

- **移动端**: React Native (Expo) + Zustand + React Query
- **API 层**: Node.js (Fastify) + TypeScript
- **Agent 层**: Python (FastAPI) + LangChain
- **数据库**: PostgreSQL 16 (pgvector) + Redis 7
- **LLM**: GPT-4o (高质量) / DeepSeek-V3 (成本优化)

## Monorepo 结构

```
apps/mobile   → React Native 移动端
apps/api      → Fastify API 服务 (TypeScript)
apps/agent    → FastAPI Agent Runtime (Python)
packages/     → 共享类型、UI Kit、工具函数
infra/        → Docker、K8s、Helm 配置
```

## 核心原则

1. **所有功能导向线下见面** — 拒绝无目的闲聊功能
2. **Agent 代理社交** — 用户不直接对话，Agent 前置沟通
3. **可解释的匹配** — 每次匹配必须附带原因说明
4. **隐私优先** — 数据最小化采集，明确授权
5. **PRD.md 是唯一事实来源** — 功能设计以 PRD.md 为准
